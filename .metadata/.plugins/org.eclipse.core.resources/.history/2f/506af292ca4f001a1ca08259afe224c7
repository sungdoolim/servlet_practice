package lab.web.servlet;

import java.io.IOException;

import javax.servlet.RequestDispatcher;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import lab.web.dao.MemDAO;
import lab.web.domain.MemVO;

/**
 * Servlet implementation class Memdo
 */
@WebServlet("/Mem.do")
public class Memdo extends HttpServlet {
	private static final long serialVersionUID = 1L;
      
    /**
     * @see HttpServlet#HttpServlet()
     */
    public Memdo() {
        super();
        // TODO Auto-generated constructor stub
    }

	/**
	 * @see HttpServlet#doGet(HttpServletRequest request, HttpServletResponse response)
	 */
	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		request.setCharacterEncoding("utf-8");
		 MemDAO mem=new MemDAO();
	String name = request.getParameter("name");
	MemVO memvo=new MemVO();
	memvo = mem.selectMem(name);
	request.setAttribute("memvo", memvo);
	RequestDispatcher disp =request.getRequestDispatcher("/memvo.jsp");
	disp.forward(request, response);
	
	
	
	
	
	
	
	
	}

	/**
	 * @see HttpServlet#doPost(HttpServletRequest request, HttpServletResponse response)
	 */
	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
	
		
		
		request.setCharacterEncoding("utf-8");
		 MemDAO mem=new MemDAO();
		String name = request.getParameter("name");
		String phoneNumber = request.getParameter("phoneNumber");
		String email = request.getParameter("email");
		MemVO memvo=new MemVO();
		memvo.setName(name);
		memvo.setPhoneNumber(phoneNumber);
		memvo.setEmail(email);
		request.setAttribute("message",mem.insert(memvo));
		RequestDispatcher disp=request.getRequestDispatcher("/Memvo.jsp");
		disp.forward(request,response);
		
		
		
	}

}
